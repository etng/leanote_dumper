# script to dump your leannote notes

It dump your leanote notes and relative images and attachments to the data directory.

## usage

* sync your leanotes
* `pip install -r requirements.txt`
* `make`
* check notes in the data directory
